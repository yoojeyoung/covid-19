extends /_src/_pug/_extend_html5

block title
    title 프로토타입

block header

block contents
    #container

        h1.hide-txt 프로토타입
        #content(role='main')
            //- ******************** code comments ********************
            //- tab
            //- ******************** code comments ********************
            .____prototype-group
                h2.____prototype-tit01 탭 메뉴 - 균등분할(equal)
                br

                .tab-nav-container
                    +tab-nav(
                        'tab-nav',
                        0,
                        [
                            'ON 20px M<br> #ffffff',
                            'OFF 20px R #666666',
                            'OFF',
                            'OFF'
                        ]
                    )

            .____prototype-group
                h2.____prototype-tit01 탭 메뉴 - 불균등분할 (unequal)
                br

                .tab-nav-container
                    +tab-nav(
                        'tab-nav unequal',
                        0,
                        [
                            'ON 20px M<br> #ffffff',
                            'OFF 20px R #666666',
                            'OFF',
                            'OFF'
                        ]
                    )

                br
                br

                .tab-nav-container.scroll
                    +tab-nav(
                        'tab-nav unequal',
                        0,
                        [
                            'ON 20px M<br> #ffffff',
                            'OFF 20px R #666666',
                            'OFF',
                            'OFF'
                        ]
                    )

                //- 최종업데이트 - 2020. 09. 16.
                pre.____code-info.
                    [탭 메뉴]
                    # 태그는 기본 ul, li 이지만 필요에 따라 변경되어도 스타일은 유지 됩니다.
                    # tab-item 균등 분할 입니다.
                    # tab-item 불균등 분할은 tab-nav 에 unequal 클래스를 추가 해줍니다.
                    # tab-item 추가 및 삭제하여 탭 메뉴의 갯수를 조절 할 수 있습니다.
                    # tab-item 의 높이는 가변 입니다.
                    # tab-link 에 활성화 클래스 active

                    # 스크롤 버전은 모바일에서만 사용 합니다.
                    # 가로 스크롤 필요시 .tab-nav-container 에 scroll 클래스를 붙여줍니다.
                    # 스크롤은 .tab-nav.unequal 에서만 작동 합니다.
                pre

block footer

block script
